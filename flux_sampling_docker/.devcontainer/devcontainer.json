{
  "name": "Python 3.8 Jupyter Env",
  "build": {
    "dockerfile": "../Dockerfile"
  },
  "workspaceFolder": "/app",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "ms-toolsai.jupyter"]
    },"settings": {
        "terminal.integrated.cwd": "/app" // <--- ADD THIS: Keeps terminal in sync
      }
  }
}
